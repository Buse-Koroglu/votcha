services:
  frontend:
    build:
      context: ../frontend/votcha-client/.
      args:
        MODE: docker
    container_name: votcha-frontend
    ports:
      - "${FRONTEND_PORT}:80"
      - "443:443"
    depends_on:
      app:
        condition: service_healthy