apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: votcha-dev
spec:
  selector:
    app: votcha
    component: logstash
  ports:
    - name: beats
      port: 5044
      targetPort: 5044
    - name: tcp
      port: 5000
      targetPort: 5000
