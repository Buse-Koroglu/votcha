services:
  app:
    extends:
      file: deploy/backend.yaml
      service: app
  frontend:
    extends:
      file: deploy/frontend.yaml
      service: frontend
  db:
    extends:
      file: deploy/db.yaml
      service: db
networks:
  votcha_network:
    driver: bridge
volumes:
  votcha_vol:
    driver: local