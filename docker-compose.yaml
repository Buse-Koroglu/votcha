services:
  app:
    build: .
    container_name: votcha
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${APP_PORT}:8090"
    healthcheck:
      test: "wget --spider -q http://app:8090/actuator/health || exit 1"
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 40s
    depends_on:
      db:
        condition: service_healthy
    networks:
      - votcha_network
    volumes:
      - ./logs:/app/logs
  frontend:
    build:
      context: ./frontend/votify-meet-client/.
      args:
        MODE: docker
    container_name: votcha-frontend
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      app:
        condition: service_healthy
    networks:
      - votcha_network
  db:
    container_name: votcha_db
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - votcha_vol:/var/lib/postgresql/data
    ports:
      - "${DB_PORT}:5432"
    networks:
      - votcha_network
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d votcha" ]
      interval: 30s
      timeout: 10s
      retries: 5
networks:
  votcha_network:
    driver: bridge
volumes:
  votcha_vol:
