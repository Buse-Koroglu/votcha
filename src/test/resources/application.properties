spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=postgres
spring.datasource.password=enes
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# 7 days, for refresh token
jwt.refresh-token.expiration-in-seconds=604800

# You need to set this as true in production environment
app.security.cookie.secure=false

initial.admin.email=admin@votcha.com
initial.admin.password=superadmin

spring.data.elasticsearch.repositories.enabled=false
spring.elasticsearch.crt.path={CRT_PATH:/home/enes/Documents/votcha/certs/ca.crt}